# AutoMedic Mission Control — env template
# Copy to .env.local for local overrides (optional until live webhooks exist).

# Leave empty to use /mock/state.json (bundled contract from issue #1)
VITE_AUTOMEDIC_BASE_URL=
VITE_AUTOMEDIC_SECRET=
VITE_USE_MOCK=true
